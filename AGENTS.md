# rlm-deal-desk

Quote compare, margin flags, and the Deal Desk Assistant agent.

Read `README.md` first. To point this package at another catalog, follow `.cursor/skills/adapt-deal-desk/SKILL.md`. Do not deploy Network metadata (`*.network-meta.xml`).

This package was extracted from a Revenue Cloud demo. It deploys onto an org that already has Revenue Cloud. It does not include the foundations CumulusCI project.
